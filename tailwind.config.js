/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./**/*.{tsx,ts,css}'],
    darkMode: 'class', // or 'media' or 'class'
    theme: {
        extend: {},
    },
    plugins: [],
}
