import { Component } from '@angular/core'
import { IconComponent } from '../icon.component'

@Component({
    selector: 'upup-onedrive-icon',
    standalone: true,
    imports: [IconComponent],
    template: `<upup-icon name="one-drive" />`,
})
export class OneDriveIconComponent {}
