import { nothing } from 'lit-html'
/** Image editor is not supported in @upup/vanilla v1. Use @upup/react for image editing. */
export function imageEditorStub(): typeof nothing {
    return nothing
}
