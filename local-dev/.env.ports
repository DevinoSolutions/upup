# UpUp local port defaults (can be overridden per developer)
PORT=53000
LANDING_PORT=53000
DOCS_PORT=53002
UPUP_DEV_SERVER_PORT=53010
STORYBOOK_PORT=53050
# 53056 sits inside the OAuth clients' registered redirect range (53050-53060)
# and clear of the six cf storybooks (53050-53055) and the presign harness
# (53060), so drive popups work against the local playground.
PLAYGROUND_PORT=53056
