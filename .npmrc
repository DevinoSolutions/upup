# pnpm publish (invoked by `changeset publish`) must not block on branch or
# working-tree git checks — the release runs post-merge in CI on master.
# Restates the historical per-package `pnpm publish --no-git-checks`.
git-checks=false
